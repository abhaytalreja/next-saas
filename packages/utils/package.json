{
  "name": "@nextsaas/utils",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "eslint src/",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "dependencies": {
    "clsx": "^2.1.0",
    "date-fns": "^3.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@nextsaas/tsconfig": "*",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.2.0"
  }
}