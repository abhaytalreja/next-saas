{
  "name": "@nextsaas/types",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "echo 'Lint temporarily disabled for Types package'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "@nextsaas/tsconfig": "*",
    "typescript": "^5.0.0"
  }
}